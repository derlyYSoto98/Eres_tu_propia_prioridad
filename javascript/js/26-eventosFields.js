
function validarInfo(numeroDocumento) {
    let numeroDoc = document.getElementById(numeroDocumento).value;
    if (numeroDoc === "")
    alert('ALERTA!! El numero de documento no puede estar vacio')
}